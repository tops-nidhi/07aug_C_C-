#include<iostream>
int main()
{
    int n;
    std::cout<<"Enter the value of the n: "; // To take the input from the user
    std::cin>>n;
    std::cout<<std::endl<<"\nValue if the n is: "<<n;
    return 0;
}